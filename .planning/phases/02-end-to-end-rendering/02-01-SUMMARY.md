---
phase: 2
plan_id: 01
title: Generate SDK Bindings
subsystem: SpaceTimeDB Client Integration
tags:
  - sdk-generation
  - typescript
  - spacetimedb
  - auto-binding
dependency_graph:
  requires: []
  provides:
    - TypeScript SDK bindings for all tables
    - Reducer type definitions
    - Table type schemas
tech_stack:
  added:
    - spacetimedb@2.0.2 (SDK library)
  patterns:
    - Generated TypeScript bindings from SpaceTimeDB CLI
key_files:
  created:
    - client/src/module_bindings/index.ts (main SDK exports)
    - client/src/module_bindings/types.ts (type definitions)
    - client/src/module_bindings/player_table.ts (Player table schema)
    - client/src/module_bindings/ship_table.ts (Ship table schema)
    - client/src/module_bindings/missile_table.ts (Missile table schema)
    - client/src/module_bindings/fire_missile_reducer.ts (action binding)
    - client/src/module_bindings/register_player_reducer.ts (action binding)
    - client/src/module_bindings/set_waypoint_reducer.ts (action binding)
    - client/src/module_bindings/spawn_ship_reducer.ts (action binding)
    - client/src/module_bindings/toggle_radar_reducer.ts (action binding)
    - client/src/module_bindings/types/procedures.ts (procedure schemas)
    - client/src/module_bindings/types/reducers.ts (reducer type definitions)
  modified:
    - client/package.json (added spacetimedb@2.0.2 dependency)
    - client/pnpm-lock.yaml (dependency lock update)
decisions:
  - Used spacetimedb@2.0.2 to match SpaceTimeDB CLI generator version (2.0.2)
  - Generated bindings target TypeScript with full type safety
metrics:
  duration_minutes: 3
  completed_date: 2026-04-25T21:48:35Z
  tasks_completed: 1
  files_created: 12
  files_modified: 2
---

# Phase 2 Plan 01: Generate SDK Bindings Summary

**Goal:** TypeScript SDK bindings from SpaceTimeDB module with full table and reducer type definitions.

## Execution Summary

Successfully generated TypeScript SDK bindings from the Iron Admiral SpaceTimeDB backend module. The command `spacetime generate --lang typescript --out-dir client/src/module_bindings --module-path server/spacetimedb` completed successfully and generated all required type definitions and API bindings.

## Tasks Completed

### Task 1: Generate SDK bindings with SpaceTime CLI ✓

The SpaceTimeDB code generation command executed without errors:

```bash
spacetime generate --lang typescript --out-dir client/src/module_bindings --module-path server/spacetimedb
```

**Output:**
- Compiled iron_admiral v0.1.0 (Rust backend)
- Generated 12 TypeScript binding files
- Build finished successfully
- Note: wasm-opt optimization not available but bindings generated correctly

### Task 2: Verify generated files ✓

All required files were created in `client/src/module_bindings/`:

**Main exports (index.ts):**
- DbConnection class with typed access to tables and reducers
- SubscriptionBuilder for real-time subscriptions
- EventContext types for event handling

**Table type definitions:**
- `player_table.ts` - Player table with identity, nickname, online status
- `ship_table.ts` - Ship table with full kinematics, waypoint, radar state
- `missile_table.ts` - Missile table with tracking and targeting data

**Reducer bindings:**
- `fire_missile_reducer.ts` - Launch missiles action
- `register_player_reducer.ts` - Player registration action
- `set_waypoint_reducer.ts` - Set ship navigation waypoint action
- `spawn_ship_reducer.ts` - Create new ship action
- `toggle_radar_reducer.ts` - Toggle radar on/off action

**Type schemas:**
- `types.ts` - Core type definitions (Player, Ship, Missile, ShipClass enum, Waypoint)
- `types/procedures.ts` - Procedure schemas
- `types/reducers.ts` - Reducer type definitions

## Verification & Build

**TypeScript Compilation:** PASSED

Initial compilation showed `spacetimedb` module was missing from dependencies. This was auto-fixed by:
1. Adding `spacetimedb@2.0.2` to `client/package.json` dependencies
2. Running `pnpm install` to update lock file
3. Verified full client build succeeds: `tsc && vite build` ✓

The version constraint (2.0.2) ensures compatibility with the generated code (generated by SpaceTimeDB CLI v2.0.2).

## Deviations from Plan

### Auto-fixed Issues

**[Rule 2 - Missing Critical Functionality] Missing spacetimedb SDK dependency**
- **Found during:** TypeScript compilation verification
- **Issue:** Generated binding files import from "spacetimedb" but the package was not in client dependencies, causing 16 TypeScript compilation errors
- **Fix:** Added `spacetimedb@2.0.2` to client/package.json dependencies (matched generator version), ran pnpm install
- **Files modified:** client/package.json, client/pnpm-lock.yaml
- **Commit:** 47ff108

## Success Criteria Met

- [x] `spacetime generate` command completes successfully
- [x] `client/src/module_bindings/` directory created with all generated files
- [x] Generated files contain Player, Ship, and Missile table types (verified in types.ts and table files)
- [x] No TypeScript compilation errors in generated code (full build passes)
- [x] All 5 reducer actions properly bound
- [x] All table indexes and constraints reflected in generated schemas

## Key Generated Exports

From `client/src/module_bindings/index.ts`, the generated SDK provides:

```typescript
// Tables (query builders)
export const tables: QueryBuilder<typeof tablesSchema.schemaType>

// Reducers (action dispatchers)
export const reducers = {
  fire_missile,
  register_player,
  set_waypoint,
  spawn_ship,
  toggle_radar
}

// Database connection class
export class DbConnection extends DbConnectionImpl<typeof REMOTE_MODULE>

// Subscription builder for real-time updates
export class SubscriptionBuilder extends SubscriptionBuilderImpl<typeof REMOTE_MODULE>
```

## Next Steps

Generated SDK bindings are ready for integration with the client UI. The bindings provide:
- Fully typed access to Player, Ship, and Missile tables
- Type-safe reducer calls for all game actions
- Real-time subscription support for reactive updates
- Event context types for event handling

Can now proceed with Phase 2-02 (Implement Client-Server Communication) to wire these bindings into the frontend.

## Self-Check: PASSED

- [x] Created files exist:
  - client/src/module_bindings/index.ts ✓
  - client/src/module_bindings/types.ts ✓
  - client/src/module_bindings/player_table.ts ✓
  - client/src/module_bindings/ship_table.ts ✓
  - client/src/module_bindings/missile_table.ts ✓
  - (and 7 more binding files)

- [x] Commits exist:
  - 47ff108: feat(phase-2-01): generate typescript SDK bindings ✓

- [x] TypeScript compilation: PASSED ✓

- [x] No known stubs or incomplete implementations ✓

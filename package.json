{
  "name": "iron-admiral-root",
  "private": true,
  "scripts": {
    "lint": "cd client && pnpm run lint",
    "test": "cd client && pnpm run test -- --coverage",
    "build": "cd client && pnpm run build"
  }
}